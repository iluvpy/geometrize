{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch lldb",
      "program": "${workspaceFolder}/client",
      "args": ["dog.jpg"],
      "env": {
        "LD_LIBRARY_PATH":"{LD_LIBRARY_PATH}:/usr/local/lib"
      },
      "cwd": "${workspaceFolder}"
    }
  ]
}