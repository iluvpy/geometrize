{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch lldb",
      "program": "${workspaceFolder}/geometrize",
      "args": ["minecraft.jpg"],
      "cwd": "${workspaceFolder}"
    }
  ]
}